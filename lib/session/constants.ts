/** The sole localStorage key for the structured MVP design session. */
export const DESIGN_SESSION_STORAGE_KEY = "webmcp.design-session.v1";

/** Small MVP history bound; only the newest versions are retained. */
export const SITE_VERSION_HISTORY_LIMIT = 10;
